export enum Parts {
    playlistItems = "",
    channel = "contentDetails,snippet,statistics,status",
    video = "",
    playlist = "",
    search = ""
}

export enum Endpoints {
    playlistItems = "playlistItems",
    channel = "channels",
    video = "videos",
    playlist = "playlists",
    search = "search"
}
